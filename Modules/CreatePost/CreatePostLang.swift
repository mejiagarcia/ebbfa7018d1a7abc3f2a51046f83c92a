import Foundation

struct CreatePostLang {
    static let title = "Create Post"
    static let shareButton = "Share"
    
    struct Error {
        static let title = "Error"
        static let message = "Invalid text"
    }
}
